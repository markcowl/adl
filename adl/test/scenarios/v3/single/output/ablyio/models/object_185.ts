export interface object_185 {
    /**
     * @description A signature, generated as an HMAC of each of the above components, using the key secret value.
     */
    mac?: unknown /*= (not tsschema -- undefined[object Object]/undefined ) =*/;
}
