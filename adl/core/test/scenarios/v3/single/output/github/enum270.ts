export enum enum270 {
    error = 'error',
    failure = 'failure',
    inactive = 'inactive',
    in_progress = 'in_progress',
    queued = 'queued',
    pending = 'pending',
    success = 'success'
}
