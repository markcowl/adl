export enum enum390 {
    author_id = 'author_id',
    'referenced_tweets.id' = 'referenced_tweets.id',
    in_reply_to_user_id = 'in_reply_to_user_id',
    'geo.place_id' = 'geo.place_id',
    'attachments.media_keys' = 'attachments.media_keys',
    'attachments.poll_ids' = 'attachments.poll_ids',
    'entities.mentions.username' = 'entities.mentions.username',
    'referenced_tweets.id.author_id' = 'referenced_tweets.id.author_id'
}
