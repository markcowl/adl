/** @description The vault's create mode to indicate whether the vault need to be recovered or not. @todo-temporary-reuse-marker */
export enum CreateMode {
    recover = 'recover',
    default = 'default'
}
